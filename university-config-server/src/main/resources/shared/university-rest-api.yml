security:
  oauth2:
    client:
      clientId: university-rest-api
      clientSecret: password # ${NOTIFICATION_SERVICE_PASSWORD}
      accessTokenUri: http://localhost:5000/uaa/oauth/token
      grant-type: client_credentials
      scope: server

server:
  servlet:
    context-path: /api
  port: 8081
