spring:
  application:
    name: university-gateway-server
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: password # ${CONFIG_SERVICE_PASSWORD}
      username: user
